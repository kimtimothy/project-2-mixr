<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Document</title>
</head>
<body>
    <%- include("./partials/navbar.ejs") %>
    <header>
        <h1>Home Page</h1>
            <nav>
                <ul>
                    <li>
                        <a href="/users">Users</a>
                    </li>
                    <li>
                        <a href="/contents">Contents</a>                    
                    </li>
                </ul>
            </nav>
    </header>
    <main>
        <ul>
        <% for(let i =0; i < users.length; i++) {%>
            <% for(let j = 0; j < users[i].content.length; j++) { %>
                <li>
                    <a href="/contents/<%=users[i].content[j]._id%>"><%=content[j].url%></a>
                </li>  
            <% } %>
        <%}%>
        </ul>
    </main>
</body>
</html>